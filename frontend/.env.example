# Local (optional — without this, browser uses hostname:4000)
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Production (Vercel) — set in Vercel dashboard:
# NEXT_PUBLIC_API_URL=https://api.socialvelocityy.com/api
